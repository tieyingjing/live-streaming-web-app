// 导出 mediasoup-client 为全局变量
import * as mediasoupClient from 'mediasoup-client';

export default mediasoupClient;
export const Device = mediasoupClient.Device;
export const types = mediasoupClient.types;
